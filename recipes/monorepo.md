# Monorepo Recipe

Detect package boundaries:
- `apps/*`
- `packages/*`
- `services/*`
- `crates/*`
- workspace config files

Only create nested `AGENTS.md` for real package/service/architecture boundaries.
